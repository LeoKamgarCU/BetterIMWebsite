<%- include ('../partials/header') %>
  <%- include ('../partials/menu') %>

    <main>
      <div class="col">
        <%- include ('../partials/message') %>
      </div>
      <div class="container p-4">
        <!-- THE REST OF YOUR HTML CODE GOES HERE -->
        <h1>Discover sports</h1>
        <%sports.forEach(function(sport, index){ 
          let hrefName = "/teams/"+sport.sportname;
          if((index%2) ==0){%>
          <div class="row p-2">
            <%}%>
              <div class="col sm-6">
                <div class="card bg-dark text-white h-100">
                  <div class="card-body d-flex flex-column">
                    <img class="card-img-top" object-fit="cover" src="" alt="Card image">
                    <div class="container mt-auto">
                      <h5 class="card-title">
                        <%=sport.sportname%>
                      </h5>
                      <p class="card-text">Description:
                        <!-- Maybe add description to sports db? eg. sport.description -->
                      </p>
                      <p class="card-text">Number of teams:
                        <!-- Use teamsToSports db table -->
                      </p>
                    </div>

                    <a href= <%= hrefName %>  class="btn btn-primary mt-auto">Join/Create Team</a>
                    <!-- Button should redirect to a page with a list of teams -->
                  </div>
                </div>
              </div>
              <%if((index%2) != 0){%>
          </div>
          <%}%>

          <%})%>



    </main>

    <%- include ('../partials/footer') %>